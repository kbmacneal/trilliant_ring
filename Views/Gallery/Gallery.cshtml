@{ ViewData["Title"] = "Trilliant Ring Gallery"; }

<div class="container-fluid" style="font-size: 16pt;">
    @Html.Partial("Dropdown")
    <div class="text-center">
        @Html.Raw(Model.row_html)
    </div>
    
</div>